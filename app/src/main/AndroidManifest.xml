<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.esraahosny.graduationapp">

     <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/food_icon"
        android:supportsRtl="true"
        android:label="Cafeteria"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Auth.LoginActivity"
            android:label="Login"/>
        <activity android:name=".Auth.SignupActivity"
            android:label="Sign Up"/>
        <activity android:name=".Activities.SettingsActivity"
            android:label="Settings"/>
        <activity android:name=".Activities.CategoriesActivity"
            android:label="Category"/>
        <activity android:name=".Activities.MenuItemsActivity"
            android:label="MenuItems"/>
        <activity android:name=".Activities.MenuItemDetailsActivity"
            android:label="Item Details"></activity>
    </application>

</manifest>